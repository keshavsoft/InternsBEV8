💡 Sort :

📌 **1.AsIs** = A GET API that retrieves all data from the table without any filtering or modification.

📌 **2.AsIsReverse** = Returns a single row from the table based on the provided primary key (PK).
